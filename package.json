{
  "name": "apollo-fetch",
  "version": "1.0.0",
  "engines": {
    "node": "12.18.2",
    "yarn": "1.22.4"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/cccole3/apollo-fetch"
  },
  "author": "Chris Cole",
  "license": "MIT",
  "description": "Scalable, simple URL shortener with user accounts and more!",
  "scripts": {
    "deploy": "git push heroku main",
    "logs:prod": "heroku logs --tail",
    "dev": "concurrently -k -p \"[{name}]\" -n \"Server,Client\" -c \"green.bold,cyan.bold\" \"cd server && yarn dev-server\" \"cd client && yarn start\"",
    "build:server": "cd server && yarn && npm run build",
    "build:client": "cd client && yarn && npm run build --prod",
    "build": "yarn build:server",
    "start": "yarn build:client && cd server && yarn serve"
  },
  "devDependencies": {
    "@types/express-flash": "^0.0.2",
    "@types/lusca": "^1.6.2",
    "@types/passport": "^1.0.4",
    "concurrently": "^5.3.0"
  },
  "dependencies": {
    "express-flash": "^0.0.2",
    "lusca": "^1.6.1",
    "passport": "^0.4.1"
  }
}
